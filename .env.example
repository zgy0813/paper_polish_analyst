# AI学术论文风格分析与润色系统 - 环境变量配置示例

# ===================
# AI API 配置
# ===================

# AI提供商选择: 'openai' 或 'deepseek'
AI_PROVIDER=openai

# ===================
# OpenAI 配置
# ===================

# OpenAI API密钥 (必需) - 请替换为您的实际API密钥
OPENAI_API_KEY=sk-your_openai_api_key_here

# OpenAI模型选择 (可选，默认为gpt-4)
# 推荐模型: gpt-4, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL=gpt-4

# OpenAI API基础URL (可选，默认为官方API)
# 如果使用代理或自定义端点，请修改此URL
OPENAI_BASE_URL=https://api.openai.com/v1

# ===================
# DeepSeek 配置 (备用)
# ===================

# DeepSeek API密钥 (如果使用DeepSeek作为备用)
DEEPSEEK_API_KEY=sk-your_deepseek_api_key_here

# DeepSeek模型选择 (可选，默认为deepseek-chat)
DEEPSEEK_MODEL=deepseek-chat

# DeepSeek API基础URL (可选，默认为官方API)
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# ===================
# AI 通用参数配置
# ===================

# 最大生成token数 (可选，默认4000)
# 根据您的需求调整，更大值可以生成更长的分析结果
AI_MAX_TOKENS=4000

# 温度参数 (可选，默认0.3)
# 0.0-1.0之间，0.3适合分析任务，0.7适合创意任务
AI_TEMPERATURE=0.3

# ===================
# ===================
# 分析配置
# ===================

# 每批分析的论文数量 (可选，默认10)
# 根据您的API限制和内存情况调整
BATCH_SIZE=10

# 最大论文数量 (可选，默认100)
# 限制单次分析的最大论文数量
MAX_PAPERS=100

# ===================
# 规则分类阈值配置
# ===================

# 高频规则阈值 (60%+论文遵循)
# 超过此阈值的规则将被标记为高频规则
FREQUENT_RULE_THRESHOLD=0.6

# 常见规则阈值 (30%-60%论文遵循)
# 在此范围内的规则将被标记为常见规则
COMMON_RULE_THRESHOLD=0.3

# 替代规则阈值 (10%-30%论文遵循)
# 在此范围内的规则将被标记为替代规则
ALTERNATIVE_RULE_THRESHOLD=0.1

# ===================
# 停止条件配置
# ===================

# 最少批次数 (用于规则多样性判断)
# 至少需要分析这么多批次才开始判断是否停止
MIN_BATCHES_FOR_DIVERSITY=8

# 最多批次数 (用于规则多样性判断)
# 最多分析这么多批次就强制停止
MAX_BATCHES_FOR_DIVERSITY=15

# 规则多样性阈值 (0.9表示90%的规则多样性)
# 当规则多样性达到此阈值时停止分析
RULE_DIVERSITY_THRESHOLD=0.9

# ===================
# 文件路径配置
# ===================

# 数据目录 (可选，默认为data)
# 所有数据文件将存储在此目录下
DATA_DIR=data

# ===================
# 使用说明
# ===================

# 1. 复制此文件为 .env: cp .env.example .env
# 2. 编辑 .env 文件，填入您的实际API密钥
# 3. 根据需要调整其他参数
# 4. 确保 .env 文件已添加到 .gitignore 中，避免泄露API密钥

# 重要提示:
# - 请妥善保管您的API密钥，不要将其提交到版本控制系统
# - 建议定期轮换API密钥以确保安全
# - 如果使用OpenAI API，请注意API使用费用
